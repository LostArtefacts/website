{# def assets, tag=None #}
<ul class='download-links no-bullets'>
  {% for asset in assets %}
    {% if not tag or tag in asset.name|lower %}
      <li>
        <DownloadLink asset={{ asset }}/>
      </li>
    {% endif %}
  {% endfor %}
</ul>
